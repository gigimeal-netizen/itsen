# Copy to .env for local overrides. In most hosting platforms (Render,
# Fly.io, Railway, etc.) PORT is injected automatically — you usually don't
# need to set this yourself in production.
PORT=2567
